# Copy this file to .env and fill the values before running the app
# Database connection
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=rosewood_db
DB_PORT=3306

# JWT secret for authentication (use a long random string in production)
JWT_SECRET=change_this_to_a_long_random_secret

# Nuxt runtime config (if needed)
# NUXT_APP_API_BASE=/api
